#ifndef CONFIG_H
#define CONFIG_H

#define WIDTH (HEIGHT * RATIO)
#define HEIGHT 800
#define RATIO 1.2 // Aspect ratio
#define SCALE 1 // Scaling factor for the renderer

#define RENDER_TO_FILE false // Set to true to render to a file instead of the screen

#endif
